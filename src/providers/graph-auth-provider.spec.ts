import { GraphAuthProvider } from './graph-auth-provider';

describe('GraphAuthProvider', () => {
  it('should create an instance', () => {
    expect(new GraphAuthProvider()).toBeTruthy();
  });
});
